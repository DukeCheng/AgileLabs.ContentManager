@model Resource
@{
    ViewData["Title"] = "MiniAdmin HomePage";
}


<form asp-action="@(Model==null?"Create":"Edit")" enctype="multipart/form-data" method="post">
    @if (Model != null)
    {
        <input type="hidden" name="@nameof(Model.Id)" value="@Model.Id" />
    }
    @*<div class="form-group">
        <label for="exampleInputEmail1">Email address</label>
        <input type="email" class="form-control" id="exampleInputEmail1" placeholder="Email">
        </div>
        <div class="form-group">
        <label for="exampleInputPassword1">Password</label>
        <input type="password" class="form-control" id="exampleInputPassword1" placeholder="Password">
        </div>*@
    <div class="form-group">
        <label for="file">File input</label>
        <input type="file" id="file" name="file" mulpitle>
        <p class="help-block">Example block-level help text here.</p>
    </div>
    <div class="checkbox">
        <label>
            <input type="checkbox" asp-for="Enabled"> Check me out
        </label>
    </div>
    <input asp-for="Path" class="form-control" />
    <button type="submit" class="btn btn-default">Save</button>
</form>